# CtrlSpeak 0.7.4 — optional Ubuntu GPU cleanup

## Implementation and deployment ledger

Requested 2026-09-15. Status: released and deployed to the reachable desktop,
gateway and Ubuntu host. Windows laptop remains pending because SSH is offline.

Baseline: published desktop and Ubuntu worker 0.7.3, source branch `v0.7`
at `50b50714`; dedicated gateway reports an unpublished 0.7.4 CPU-normalization
patch. Preserve the gateway's 115 enabled correction rules, exact overrides,
identities, credentials, and any other production changes during reconciliation.
Do not overwrite the host-specific Nova Hermes adapter.

## Agreed behavior

- Gateway remains the only client-facing API, routing and correction authority.
- Ubuntu worker optionally runs S1-mini after successful CUDA Whisper inference,
  within the same worker request. S1 runs in a warm, private CUDA llama.cpp service.
- Client/API `cleanup` is an explicit boolean, default false for compatibility.
  A persistent desktop checkbox opts into cleanup; the user's desktop will be
  configured GPU-preferred with cleanup enabled during deployment.
- OpenAI and gateway-tiny routes never call S1. Disabled cleanup does no cleanup
  work. Worker cleanup failure returns the successful Whisper result, without
  triggering another transcription attempt.
- Keep `raw_text` (ASR) and `text` (ordinary gateway-corrected text) compatible.
  Return optional `normalized_text` plus bounded normalization metadata, including
  requested/applied/status/model/device/location/duration. New clients select
  normalized text only on a valid applied response for their requested cleanup.
- Apply known-word rules to the selected cleaned variant at the gateway. Exact
  user-confirmed overrides take precedence. Never send OpenAI keys to the worker.
- Fail-open cleanup: bounded input/output, bounded queue and inference deadline,
  English-only, safe output checks. No gateway CPU cleanup, retries or model loads
  on the interactive path. Preserve worker health/circuit-break failover bounds.
- UI explains applied, off, unavailable, unsupported provider, unchanged, and
  timeout outcomes accurately. Preference survives an offline Ubuntu PC.

## Work sequence / acceptance

1. Inspect live production patches and capture rollback/source/database copies.
2. Reconcile useful server-only patches into reviewed Git source, implement the
   worker-only normalizer, transport, strict metadata and backward compatibility.
3. Implement persisted desktop preference, request propagation, result selection,
   and honest status. Add unit, API, routing, failure, auth and regression tests.
4. Test serially with hard timeouts; compile and inspect the diff. Update README,
   API, service, build, test and updater docs; keep historical release notes intact.
5. Provision/benchmark the CUDA S1 service on Ubuntu. Stage the versioned service
   source and verify actual GPU offload and memory headroom before cutover.
6. Deploy worker, then gateway; disable gateway S1 CPU service/timer only after
   successful replacement. Verify corrections unchanged, GPU on/off cleanup,
   OpenAI/tiny bypass, cleanup failure and worker-offline fallback.
7. Commit/push 0.7.4, build native Windows/Linux packages, publish an immutable
   signed five-asset GitHub release, independently verify it, and update the stable
   desktop executable with rollback. Verify saved settings/key and GUI state.
8. Update reachable other clients if safely accessible; explicitly record any
   offline/unreachable device. Provide Nova with the new API integration contract
   and required routing/preference changes rather than silently reconfiguring it.

## Evidence

- Windows Python 3.12 core suite: 306 passed, 1 GUI integration skip,
  6 deselected. Native Ubuntu Python 3.11 service suite: 66 passed.
- RTX 3060 S1 tests: 92.339 ms and 55.344 ms; CUDA process uses 752 MiB,
  approximately 8,666 MiB free while Whisper remains loaded.
- Pinned llama.cpp b10985 CUDA 12.8 assets and S1 Q4_K_M model hashes are
  enforced by `scripts/stage_s1_gpu.py`. Upstream binaries require newer glibc
  than the host's 2.35; use `Dockerfile.s1-runtime`, not a host glibc upgrade.
  The Ubuntu 24.04 base digest is pinned in Git. Built runtime image on this
  worker: `sha256:ba4b61ae7fafd28d1365960fa37e7bbbbd5cf2b2d309eacedc9c11de9b38ddbd`.
- S1 and Whisper user services are enabled, active and GPU-verified. Gateway is
  active with its original private firewall rules. Old gateway CPU S1 and its
  health timer are disabled and inactive; loopback 8081 no longer listens there.
- Laptop `10.83.233.5` SSH timed out repeatedly, including the final retry. No
  laptop installation or credential changes were claimed or attempted offline.
- Desktop source preview launches. Windows capture API currently fails with
  `SetIsBorderRequired / E_NOINTERFACE`; accessibility exposes Tk panes only.
  Do not claim completed visual screenshot acceptance. A native in-process
  widget test passed: checkbox mapped and within the visible page, toggle and
  restoration work, and Corrections navigation selects the correct notebook
  tab. No settings were saved by that test.

## Release and deployment receipt

- Immutable code/tag: `v0.7.4`, commit
  `268c1ed44ecead0878a7449dd046d6dbe4519bd4`.
- GitHub `v0.7` and `codex/gpu-cleanup-v0.7.4` contain the reviewed change.
  Post-release documentation adds this receipt without moving the tag.
- GitHub test run `34997047978`: all Windows/Linux, Python 3.11/3.12 core
  jobs and both service jobs succeeded. Signed release run `34997130589`:
  Windows build, Linux build, manifest signing, draft audit and publication
  all succeeded.
- Public release: https://github.com/Wouter-Bekker-AI/CtrlSpeak/releases/tag/v0.7.4
  Exactly five public assets; all five independently downloaded and audited
  with `scripts/release.py verify`. Previous-version discovery validated the
  signature; a 0.7.4 check returns `up_to_date`.
- Windows artifact: 224,709,651 bytes,
  SHA-256 `0a52424f6cf2876d60c7ba751b367eba71449d7df8291c55b42157ff4a8cf233`.
- Linux artifact: 268,252,408 bytes,
  SHA-256 `81de18b117082e37775c79ded1a7c95efbaecc12b789e85954a73ecbb1939688`.
- Gateway source is a sparse Git checkout at
  `/opt/ctrlspeak-releases/268c1ed44ecead0878a7449dd046d6dbe4519bd4/server/whisper_transcription`.
  A `zz-v074-release.conf` systemd drop-in selects it while retaining the old
  virtual environment and protected data/configuration. Original application
  remains at `/opt/ctrlspeak-gateway` for rollback.
- Gateway consistent SQLite/configuration backup:
  `/var/backups/ctrlspeak/pre-v074-20260915T164443Z`. All 115 correction rules
  and the one exact transcript override were independently compared with the
  backup: configuration unchanged.
- Worker source uses the same commit under
  `/home/trueai/services/ctrlspeak-releases/268c1ed44ecead0878a7449dd046d6dbe4519bd4/server/whisper_transcription`.
  Original runtime remains at `/home/trueai/services/whisper-transcription`;
  configuration backup at
  `/home/trueai/.config/CtrlSpeak/backups/pre-v074-20260915T164338Z`.
- Windows stable `C:\Users\woute\Desktop\CtrlSpeak.exe` replaced from the
  signature/hash-verified published artifact. Packaged health probe passed;
  restarted app remained running and logged API backend startup on 0.7.4.
  Saved configuration is GPU preferred, cleanup enabled, English; bearer token
  and Windows Credential Manager OpenAI key remain present. Only one desktop
  CtrlSpeak executable remains. Previous executable/settings backup:
  `C:\Users\woute\AppData\Roaming\CtrlSpeak\backups\pre-v074-20260915T165714Z`.
- Ubuntu's previously v0.4 desktop executable/launchers were also updated to
  the signed Linux 0.7.4 artifact at
  `/home/trueai/.local/share/CtrlSpeak/CtrlSpeak`. Packaged health probe passed;
  GPU-preferred cleanup is enabled with the existing gateway identity. The GUI
  was not previously running and was not started. Old executable/settings and
  launchers are backed up under
  `/home/trueai/.config/CtrlSpeak/backups/desktop-pre-v074-20260915T165848Z`.
  No OpenAI key was copied to the worker host.

## Live routing/failure evidence

All audio tests used a generated, non-personal 4.9-second sentence, never a
user's historical voice recording. These are measured examples, not latency
guarantees for longer audio or a congested network.

| Case | Result |
|---|---|
| GPU + cleanup | Desktop selected cleaned variant; 797 ms end-to-end, 63.429 ms cleanup |
| GPU, cleanup off | Ordinary transcript; disabled status; no cleaned variant |
| OpenAI, cleanup requested | Successful transcription; no cleaned variant; 2,234 ms total |
| Gateway Tiny, cleanup requested | Successful CPU/int8 tiny transcription; no cleaned variant; 3,922 ms total |
| S1 stopped | Successful Whisper ASR retained; cleanup runtime error returned in 1.104 ms; S1 restored |
| Ubuntu worker stopped | Worker failure detected in 24.7 ms; OpenAI succeeded; no cleanup; 2,422 ms total |
| Worker restored | GPU cleanup applied again; S1/Whisper healthy and enabled |

The outage test had a separate 25-second service-start safeguard and a `finally`
restart. Cleanup unit tests also cover hard deadlines, cancellation/lock release,
concurrent busy skip, output bounds, unsupported language, truncated responses,
malformed metadata, old workers, exact overrides, rule accounting and key isolation.

## Nova and remaining limitations

Nova Hermes remains active and unchanged. It currently forces OpenAI and omits
cleanup, so it correctly receives ordinary text until its host-specific adapter
is updated. See `NOVA_CTRLSPEAK_0.7.4_HANDOFF.md` for the exact message/contract.
Do not re-enable the retired gateway CPU normalizer as a supposed repair.

Windows laptop installation awaits connectivity. Fresh physical hotkey/audio
injection and screenshot acceptance were not performed; automated core/API,
native widget, packaged startup, live provider selection and outage tests passed.
